# Generated / downloaded data — do not commit
# These are reproducible by running the scripts pipeline.
# Large files (GML, GeoJSON, centroid JSON) excluded to keep repo small.

inspire_raw/
inspire_centroids_*.json
geocoded_cache_*.json
postcode_centroids_*.json
ppd_transactions_*.json
*_summary.json

# Keep this .gitignore
!.gitignore
